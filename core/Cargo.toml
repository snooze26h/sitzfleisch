[package]
name = "sitzfleisch-core"
version = "0.9.2"
edition = "2021"
description = "Sitzfleisch rules engine: quota day, heartbeat guard, acceptance ledger. Platform-free."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2"
