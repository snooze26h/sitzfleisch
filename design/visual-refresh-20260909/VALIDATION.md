# 视觉更新验证记录

本轮前端基于 0.9.3 (`7585c64`) 实施。用户确认了骨白折带与朱红切面，并授权使用内置图像工具。

## 已完成的检查

- TypeScript 与 Vite 构建通过，死导出检查通过。
- 浏览器扩展 47 项测试通过；本轮改的是 HTML/CSS 与静态素材，原有消息、返回、缓存与拦截规则未更改。
- 在 Chrome 的本地 Vite 预览中，用模拟数据执行了目标分钟数键盘输入并保存（240 → 255，总目标 8h30 → 8h45）、开始学习、选择项目、两条任务输入、选择 25 分钟并开始、+10 分钟、任务勾选、暂停/继续、结束后进入休息。
- 键盘 Tab 可到达按钮，焦点为可见的 2px solid 外框。
- 检查了开始、专注、下一格、暂停、完成、历史、历史空态、设置和拦截页的视觉状态。
- 1020、1240、1440、1600 桌面宽度与 390 宽度兜底均已截图；已检查图片加载及横向溢出。1240×820 下，默认五项目的开始按钮底部为 799.75px，可见。
- 浅色工作区次要正文、占位文字与勾选框边线的对比已检查；独立审查指出的浅色下拉箭头已修正为 #665f52，对 #ded4c2 的对比为 4.30:1；复核 resolved，最终 disposition 为 ship。

## 验证边界

- 使用的是模拟数据，未改动用户实际学习记录、规则或配置。
- 拦截页截图来自生产 HTML/CSS 与素材加静态模拟文案；其功能证据来自扩展测试，不等同于在用户登录态中重新执行网站拦截。
- 未用当前用户存档启动另一份原生应用。图标补充版已在用户正常退出后替换到 `/Applications/Sitzfleisch.app`；本机应用控制工具连接失败，未自动重开，不把浏览器预览称作原生实拍。
- Impeccable 检测器缺少 HTML parser，降级为 regex。记录中的 91 条 advisory 均是对旧 DESIGN.md 的颜色、圆角与字号刻度差异；新的设计规范在视觉审查结束后依据成品记录，不把该扫描称为完整视觉验证。

## 素材

最终使用三张状态图与一张纸纹，共四张 WebP，应用引用部分合计 49,650 字节。拦截页另复用归档图。图片为明确的不透明图版；首轮棋盘格版本未进入项目引用。

精确提示词、来源路径、工具与模型信息位于 assets.json；最终图的 prompt sidecar 随文件保留。图形和纹理由内置 image_gen 生成，小尺寸导航与字标由 SVG 代码绘制。

## 独立审查

审查代理独立检查了十二张截图和相关源码，返回一项箭头对比度问题；定点修复并提交更新后的 chooser 截图后，审查代理确认 resolved，并返回最终 ship。复核只覆盖这一修复，其余范围沿用首轮审查。

## 应用包

`src-tauri/target/release/bundle/macos/Sitzfleisch.app` 已成功构建。`codesign --verify --deep --strict` 通过；首轮包的四张前端素材与随包扩展 HTML/CSS/归档图已核对。

2026-09-09 图标补充版已安装到 `/Applications/Sitzfleisch.app`。安装后的签名验证通过，二进制 SHA-256 为 `39b551127134397f1f5fbf911a80d82edce4af37217414e645d93f6c5232f5ca`，与新包一致。旧版与退出后的状态文件备份在仓库已忽略的 `backup-apps/visual-refresh-20260909-115652/`，安装后核对 preferences、history、day 均未改变。自动重开因本机控制工具连接失败而未完成，需用户手动打开。

## 项目图标补充验证

- 八个专属 SVG、旧 `code` 别名、通用图标和未知标识回退通过定点渲染检查；TypeScript、Vite 和 Tauri release 应用构建通过。
- Chrome 模拟数据预览中，科研图标选择保存成功；从“更多图标”选择旧指南针后，选中状态回显为 true，且当前旧图标保留在主选择区。
- 1240×900 与 1020×900 截图已检查；1020 宽度无横向溢出，八个选项均为 64×65。截图在 `.impeccable/review/project-icons-{1240,1020}.png`。
- 独立审查返回 `ship`，范围为本轮项目图标和选择器，无 material findings；未据此声称原生窗口或其他页面已重新完成视觉验证。
- 定点 regex detector 仅返回 19 项 advisory，无阻断项；本轮 9px 圆角与 11px 短标签是有意设计。该检查不等同于完整 HTML 或视觉审查。
- 本轮小图标由 SVG 代码绘制，未新增生成图片。Sitzfleisch、S 标识和现有中文显示名未改动，中文译名仍待用户决定。

## 深色工作区与状态折带

用户反馈运行台仍有大面积白底与方形 S 贴片后，开始、选任务、计时、暂停、休息、完成六种状态统一使用深色工作面。三张原创衍生折带分别用于准备、计时与收拢状态；使用内置 image_gen 生成，提示词与源文件映射见 `dark-focus-assets.json`。最终 WebP 均为 960×640、纯黑不透明背景，通过 screen 与渐隐遮罩融合；合计 37,892 字节。旧 S 大图不再进入生产包，导航 S 字标保留。

- TypeScript、Vite、Tauri release 与应用包签名验证通过。最终二进制 SHA-256：`542bf35a1098d30ce09ba19819c122c8ab1292221fbc89dcd8d68a90d7a2f931`。
- Chrome 模拟数据实测：选择阅读、输入两项任务、选择 25 分钟和休息 5 分钟、开始、勾选任务、暂停、继续、延长 10 分钟（计划变为 35 分钟）、结束进入 5 分钟休息。控件状态与图形 phase 回显正常。
- 检查 1240 宽的六种状态，以及 1020 宽的选择页、计时页和 1600 宽计时页；九张截图均位于 `.impeccable/review/dark-*.png`。1020 下文档和主内容没有横向溢出。
- 临时模拟系统减少动态效果时，折带与选择按钮 transition 均为 0s；模拟设置已还原。键盘焦点可见，为 2px 骨白轮廓。截图不证明动态帧率。
- 次要文字对比度 7.29:1，朱红主按钮文字 5.39:1，新增控件边线最差悬停态 3.23:1，下拉箭头 5.91:1。
- 素材来源扫描为 7 项、0 缺失。机械 detector 跑一次，1 项尺寸动画 warning 已改为固定尺寸与 transform，独立审查核对解决；其余 15 项为旧规范尺度 advisory，不据此声称完整 HTML 检查。
- 全新独立审查 `dark_focus_review` 对照用户反例、九张成品截图及源码，返回 `ship`，无 material findings。其动态与交互结论的依据为主流程记录；原生 WebView 视觉尚未实拍验证。

深色新版已于 2026-09-09 安装到 `/Applications/Sitzfleisch.app`。安装器在真实系统环境确认旧程序退出后完成备份与原子替换；旧应用及退出后的状态文件保存在 `backup-apps/visual-refresh-20260909-131401/`。新旧二进制分别与安装包、备份收据一致，已安装应用的签名验证通过；安装后核对 preferences、history、day 均未改变。自动打开仍因本机应用控制工具连接失败而未完成，需要用户手动打开；没有据此宣称原生视觉验证通过。

此前旧程序仍在运行时，安装器已在任何写入前安全停止。受限环境的 pgrep 返回码为 3，表示无法列进程，不能把它当作已退出；只有返回码 1 才表示未找到进程。后续替换继续使用真实系统环境与安装器的退出检查，不以构建成功代替安装完成。

## 移除重复推荐快捷按钮

按用户截图反馈，删除建议区的「选科研」等动态快捷按钮，并移除仅由它使用的 adopt 处理分支和样式。建议文字、下方项目选择与右侧配额选择保留。TypeScript、Vite、Tauri release、死导出与差异空白检查通过；已核对该按钮及处理分支不再存在，正常项目选择入口仍在。

用户退出后，删除版已安装到 `/Applications/Sitzfleisch.app`。旧应用与状态备份在 `backup-apps/visual-refresh-20260909-140707/`，新二进制 SHA-256 为 `9e2125051a1a310e14645c2f998a22a3d7ef81be0584c75c998093c6c3689b0b`。安装与备份二进制、签名及 preferences/history/day 均核对通过，数据未改变；自动打开工具不可用，需用户重新打开应用查看。

## 侧栏原图透明字标

将左上角简化的 SVG S 替换为原应用图标的立体 S 抠图。内置 image_gen 生成轮廓蒙版，ImageMagick 将蒙版与原图颜色边界结合后提取原像素；首轮棋盘格 RGB 结果未用于产品。缩放前所有可见像素 RGB 与原图一致，差异数为 0；外部和两处内部空隙的 alpha 抽样均为 0。最终素材为 256×318 无损 RGBA WebP，50,348 字节，按 38×48 CSS px 显示，来源见 `sidebar-mark/README.md` 与素材旁的 JSON。

已检查 Chrome 本地预览的 1240 与 1020 两种宽度：素材正常加载，字标没有横向溢出，镂空和原图材质可见。TypeScript、Vite、Tauri release、死导出、签名与素材来源检查通过。

用户正常退出后，透明字标版已安装到 `/Applications/Sitzfleisch.app`。旧应用和退出后的状态文件备份在 `backup-apps/visual-refresh-20260909-143659/`。新二进制 SHA-256 为 `085dd7a2322388700267a7c2b5771f54034e3e109909c1828785a92403d304c2`，与待安装包一致；备份二进制、已安装签名及 preferences/history/day 均核对通过，数据未改变。用户需手动重新打开应用；原生视觉仍未实拍验证。

## 2026-09-10 设置页头图案

移除设置右上角的方框调节器徽章，复用已有展开折带，作为深色页头中的渐隐背景。标题与自动保存提示位于图层之上；装饰层为 aria-hidden，pointer-events 为 none。本轮未新增图片，也未改变设置保存、项目编辑或屏蔽逻辑。

Chrome 模拟预览检查了 1240 宽项目页和 1020 宽网站屏蔽页：图片已加载，screen 与页面同色底结合后没有方框边界，较长标题和保存提示可见，未造成主内容区横向滚动。截图为 `.impeccable/review/settings-heading-{1240,1020}.png`。TypeScript、Vite、Tauri release、死导出和差异空白检查通过；机械扫描仅有 12 项既有尺度 advisory，不等同于完整视觉扫描。应用安装结果另行记录。

设置页头版已安装到 `/Applications/Sitzfleisch.app`，二进制 SHA-256 为 `93704b2468fe0dffaf3eeb1258654df2d74b611931ddd312676b1e3e4ce1cd52`。旧应用与退出后的状态文件备份在 `backup-apps/visual-refresh-20260910-004244/`；安装包与备份二进制、已安装签名及 preferences/history/day 均核对通过，数据未改变。首次安装因旧应用仍在运行而于任何写入前停止，随后在用户正常退出后完成替换；用户需手动重新打开应用，原生视觉未实拍验证。
