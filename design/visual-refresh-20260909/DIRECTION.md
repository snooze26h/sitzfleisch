# 已确认的视觉方向

用户已确认按提案整体优化前端，保留骨白折带、朱红切面与石墨基底，并同意直接使用内置图像工具，不强制指定 Image 2.5。本次生成文件的 C2PA 元数据标注 gpt-image 2.0，官方内置工具文档也说明使用 gpt-image-2。

采用代码实现布局、生成工具制作独立素材的方式。用户已委托具体布局与元素选择；桌面预览是实际可操作界面的展示，而非将生成的 UI 图片铺在页面上。

模式：Operate。现有单份计划、设置分区、项目配置与计时规则都是功能边界。

## 首屏与层次

- 主计时区域像一张骨白的折页，以深色读数呈现；朱红用于当前状态和主要操作。
- 外围的导航、配额与时间轴保留温暖的石墨色，形成主工作区与辅助信息的层次。
- 开始页使用展开折带作为主视觉；暂停和完成采用同系列状态素材。
- 计时期间的素材保持静止，文字、计时器和任务列表是操作中心。
- 避免将所有内容铺成同样尺寸的深灰框；历史、设置和拦截页采用同一形体语言。

## 专属元素

核心导航使用三个折页形体 SVG。既有自选项目图标保留兼容。

图片素材与应用图标共用骨白材质、朱红端面、侧上方柔光和统一视角。最终采用不透明的石墨/骨白背景图版；首轮并未产生真实 alpha，所以没有将棋盘格版本用于产品。图片作为独立资源接入，不把按钮、数字或正文烘焙进图像。

真实完成记录对应小折角；不新增积分、奖励等级或催促机制。

## 验证边界

基于模拟数据验证开始、专注、暂停、下一格、完成、历史空态和设置；检查 1020、1240、1600 宽度，以及小窗口的可读性。保留键盘焦点、减少动态效果设置与现有按钮行为。

正式资源需要保留生成来源、提示词和选用文件记录；应用内资产全部本地打包。

## 用户反馈修订

2026-09-09：用户认为开始页「今天的安排」大面积骨白过亮，要求改黑。该计划面已改为石墨黑与骨白文字，输入框同步深色，保留朱红开始按钮和原有布局。

2026-09-09：用户进一步要求图文融合与短文案。开始页独立S图块改为低透明度背景折带，消除底色矩形边界并向下渐隐；页头收为「落座，便是今天。」和一句副文案。设置与历史的常驻说明同步缩短，保留必要的权限、错误恢复与数据留存信息。
